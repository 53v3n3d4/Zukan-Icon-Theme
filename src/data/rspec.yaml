%YAML 1.2
---
name: RSpec
preferences:
  scope: source.ruby.rspecfile
  settings:
    icon: rspec
syntax:
  - name: Ruby (RSpec)
    scope: source.ruby.rspecfile
    hidden: true
    file_extensions:
      - .rspec
      - .rspec-local
    contexts:
      main:
        - include: scope:source.ruby
          apply_prototype: true

# Package RSpec uses source.ruby.rspec
