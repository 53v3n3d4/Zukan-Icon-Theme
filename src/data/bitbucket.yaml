%YAML 1.2
---
name: Bitbucket
preferences:
  scope: source.yaml.pipeline.bitbucket
  settings:
    icon: bitbucket
syntax:
  - name: YAML (Bitbucket)
    scope: source.yaml.pipeline.bitbucket
    hidden: true
    file_extensions:
      - bitbucket-pipelines.yml
    contexts:
      main:
        - include: scope:source.yaml
          apply_prototype: true

# Package YamlPipelines
