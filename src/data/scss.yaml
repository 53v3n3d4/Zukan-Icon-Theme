%YAML 1.2
---
name: SCSS
preferences:
  scope: source.scss
  settings:
    icon: scss
syntax:
  - name: SCSS
    scope: source.scss
    hidden: true
    file_extensions:
      - scss
    contexts:
      main:
        - include: scope:source.css
          apply_prototype: true

# Using same logo for SCSS but each has its file.
# Because tmPreferences also get file name from icon value.

# Package Sass
