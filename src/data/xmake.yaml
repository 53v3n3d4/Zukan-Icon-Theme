%YAML 1.2
---
name: XMake
preferences:
  scope: source.lua.xmake
  settings:
    icon: xmake
syntax:
  - name: Lua (XMake)
    scope: source.lua.xmake
    hidden: true
    file_extensions:
      - xmake.lua
    contexts:
      main:
        - include: scope:source.lua
          apply_prototype: true

# Package xmake: it uses source.lua.
# source.lua.make attempt to show xmake icon for xmake.lua
