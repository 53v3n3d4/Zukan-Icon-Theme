%YAML 1.2
---
name: GBIF
icons:
  - gbif-light
preferences:
  scope: source.txtree
  settings:
    icon: gbif-dark
syntax:
  - name: TextTree
    scope: source.txtree
    hidden: true
    file_extensions:
      - txt
      - txtree
    contexts:
      main: []

# Packages TextTree
# To maintain txt with `text` icon, need to use ST `Open all with current
# extension as...` since there will be no icon syntax.
