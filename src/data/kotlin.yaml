%YAML 1.2
---
name: Kotlin
preferences:
  scope: source.Kotlin
  settings:
    icon: kotlin
syntax:
  - name: Plain Text (Kotlin)
    scope: source.Kotlin
    hidden: true
    file_extensions:
      - kt
      - kts
    contexts:
      main: []

# syntax package has source.Kotlin with first letter capitalized.
# test with not syntax package it worked with all lower case.
# to do: test with package syntax and lowercase.
