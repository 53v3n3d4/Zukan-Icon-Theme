%YAML 1.2
---
name: Plist
icons:
  - plist-light
preferences:
  scope: text.plist
  settings:
    icon: plist-dark
syntax:
  - name: XML (Plist)
    scope: text.plist
    hidden: true
    file_extensions:
      - plist
      - tmLanguage
      - tmPreferences
      - tmSnippet
      - tmTheme
    contexts:
      main:
        - include: scope:text.xml
          apply_prototype: true

# Package plist
