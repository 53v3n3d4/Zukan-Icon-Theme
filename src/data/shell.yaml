%YAML 1.2
---
name: Shell
icons:
  - shell-light
preferences:
  scope: source.shell, source.shell.bash
  settings:
    icon: shell-dark
syntax:
  - name: Bash
    scope: source.shell.bash
    hidden: true
    file_extensions:
      - ash
      - bash
      - bashrc
      - sh
      - zsh
    contexts:
      main: []

# ST Package, source shell is hidden.
