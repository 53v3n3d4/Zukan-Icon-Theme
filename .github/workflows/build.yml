name: Build

on: [push]

jobs:
  build-tests:

    name: Build Tests
    runs-on: ubuntu-latest
    permissions:
      # Passing one of default ones.
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      #  Install the latest version of uv and set the python version to 3.13t
      - name: uv
        uses: astral-sh/setup-uv@v6
        with:
          python-version: 3.13
      - name: Python
        run: |
          uv python install 3.13
          uv pip install --python=3.13 pip
      - name: Install dependencies
        run: uv sync
      - name: Test with pytest
        run: uv run pytest
